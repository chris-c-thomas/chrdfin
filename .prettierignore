node_modules
dist
target
coverage
pnpm-lock.yaml
Cargo.lock
**/routeTree.gen.ts

# Project documentation and skill bundles — managed separately
.agents/
.claude/
docs/
CLAUDE.md
README.md

# Captured API response fixtures — must mirror the wire format byte-for-byte;
# reformatting them defeats the point of the fixture.
apps/desktop/src-tauri/tests/fixtures/
